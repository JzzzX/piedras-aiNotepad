# AI Notepad 开发计划（接手版）

更新时间：2026-02-27（ASR 基础链路已完成）

## 阶段 0：仓库清理与基线稳定（已完成）
- [x] 删除交接/调研类无关文件（`HANDOVER.md`、Granola 调研文档、临时渲染目录）
- [x] 修复 lint 报错并恢复 `npm run lint` 通过
- [x] 验证 `npm run build` 可通过
- [x] 修复 `POST /api/meetings` 保存逻辑，确保 `segments/chatMessages` 每次保存都同步落盘

## 阶段 1：阿里云 ASR 接入（进行中）
- [x] 增加 ASR 配置抽象：`lib/asr.ts`（模式、配置检测、状态输出）
- [x] 增加状态接口：`GET /api/asr/status`
- [x] 增加会话接口：`POST /api/asr/session`（返回 token / wsUrl / appKey）
- [x] 前端显示 ASR 当前状态（`AudioRecorder`）
- [x] 端侧 PCM 编码与分片发送（16k/16bit 单声道）
- [x] 阿里云 WebSocket 实时转写（麦克风 + 系统音频双通道）
- [x] 系统音频占位文本替换为真实云端转写文本（aliyun 模式下）
- [ ] 可选：改为服务端中转 WebSocket（当前为浏览器直连阿里云网关）

## 阶段 2（P0）：MiniMax 正式接入与稳定性
- [x] 增加 LLM Provider 抽象（Gemini 主通道 + MiniMax 回退）
- [x] 增加超时、重试、错误码分层（鉴权/限流/上游异常）
- [x] 去掉 Demo fallback 依赖：有 Key 时必须全链路走真实模型
- [x] 强化流式解析容错（SSE 分片、断流恢复、非法 JSON 跳过）
- [x] Prompt 参数化（会议类型、输出风格、是否输出行动项）
- 验收标准：
  - 连续 20 次 `enhance/chat` 请求无崩溃
  - 错误信息可读、可定位（前端有明确提示）

## 阶段 3（P1）：模板系统产品化
- [x] Prisma 增加 `PromptTemplate` 模型（系统模板 + 用户模板）
- [x] 新增模板 CRUD API（创建/编辑/删除/排序）
- [x] Chat 面板从数据库加载模板，保留 `/` 命令交互
- 验收标准：用户可创建并持久化自定义模板，重启后可恢复

## 阶段 4（P1）：导出与分享
- [x] 导出 Markdown（已具备）+ Docx（新增）
- [x] 导出内容结构统一：摘要/讨论点/决策/行动项
- [x] 预留飞书/企业微信 webhook 推送接口
- 验收标准：单会议可一键导出文档并保证中文排版可读

## 阶段 5（P2）：跨会议 Chat（全局知识问答）
- [ ] 增加跨会议检索层（先关键词召回，再升级向量检索）
- [ ] 支持按时间范围/会议标题过滤上下文
- [ ] 回答附“来源会议引用”
- 验收标准：多会议问题能返回可追溯答案与来源

## 未来两周执行顺序（建议）
1. **先做阶段 2**：完成 MiniMax 正式接入与稳定性加固（最高优先级）。
2. **接阶段 3**：模板持久化，让“/模板”真正可运营。
3. **随后阶段 4**：补齐导出能力，形成可交付闭环。
